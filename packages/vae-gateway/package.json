{
  "name": "vae-gateway",
  "version": "0.1.0-ms.0",
  "private": true,
  "description": "L1 model gateway: sole model ingress, explicit fallback chains only, recording postures, circuit breaker (5/30s → open 30s), versioned pricing tables.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "vae-foundation": "workspace:*"
  }
}
