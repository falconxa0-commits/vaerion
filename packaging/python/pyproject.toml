[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "vaerion"
version = "0.1.12-rc1"
description = "Vaerion — the AI-native development engine: deterministic runtime, broker-governed autonomy, hash-chained journals, receipts, and permanent provenance. Evidence, not branding."
readme = "README.md"
requires-python = ">=3.9"
license = { text = "Apache-2.0" }
authors = [{ name = "Auren", email = "auren@vaerion.dev" }]
keywords = ["ai", "agents", "determinism", "audit", "provenance", "journal", "local-first", "cli"]
classifiers = [
  "Development Status :: 4 - Beta",
  "Environment :: Console",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: Apache Software License",
  "Operating System :: MacOS",
  "Operating System :: POSIX :: Linux",
  "Programming Language :: Python :: 3",
  "Topic :: Software Development :: Build Tools",
]

[project.urls]
# Identity of record: LEGAL.md §1. The repository is public; the remaining
# URLs land with the Founder's publication step (risk-ledger F-5).
Repository = "https://github.com/falconxa0-commits/vaerion"
Issues = "https://github.com/falconxa0-commits/vaerion/issues"
Changelog = "https://github.com/falconxa0-commits/vaerion/blob/main/CHANGELOG.md"

[project.scripts]
vae = "vaerion.cli:main"

[tool.setuptools]
packages = ["vaerion"]
include-package-data = true

[tool.setuptools.package-data]
vaerion = ["engine/**/*.ts", "engine/**/*.json", "engine/**/*.md"]
