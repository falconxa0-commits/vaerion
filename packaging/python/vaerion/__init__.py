"""Vaerion — the AI-native development engine (PyPI delivery channel).

Python is the delivery channel, not the substrate: the engine is
TypeScript executed by the Bun runtime (ADR-0018). This package installs
the `vae` console launcher and the packaged engine source.
"""

__version__ = "0.1.7-rc2"
