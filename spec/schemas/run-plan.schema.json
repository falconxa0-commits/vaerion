{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://vaerion.dev/schemas/run-plan.schema.json",
  "title": "Vaerion Run Plan (Stage 11, E1008)",
  "description": "A DECLARED run plan: an acyclic DAG of steps invoking registered tools. Work is executed only as declared (D11.2, D11.3).",
  "type": "object",
  "required": ["name", "steps"],
  "additionalProperties": false,
  "properties": {
    "name": { "type": "string", "pattern": "^[a-z][a-z0-9-]{0,62}$" },
    "description": { "type": "string" },
    "steps": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["id", "tool"],
        "additionalProperties": false,
        "properties": {
          "id": { "type": "string", "pattern": "^[a-z][a-z0-9-]{0,62}$" },
          "tool": {
            "type": "string",
            "pattern": "^[a-z][a-z0-9.-]{1,62}$",
            "description": "A registered tool name (D16.1). Unregistered tools are refused (E2005)."
          },
          "input": { "type": "object", "description": "Validated against the tool's declared input schema (D16.2)." },
          "needs": {
            "type": "array",
            "items": { "type": "string" },
            "description": "Step ids this step depends on. Cycles are refused (E1008)."
          },
          "failurePolicy": {
            "enum": ["fail", "retry", "skip"],
            "description": "Retries follow the tool's declared retry policy; the engine never improvises (D16.10)."
          }
        }
      }
    }
  }
}
